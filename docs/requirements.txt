# 桌面电子宠物项目依赖
# Python 3.13
# 安装命令（虚拟环境）：pip install -r docs/requirements.txt

# ========================= 核心运行时 =========================
# GUI 框架（含 QtMultimedia，用于播放音效 / GIF）
PySide6>=6.7.0,<7

# HTTP 网络请求库（爬虫模块、ihan 等后期扩展）
requests>=2.31.0,<3

# 浏览器自动化（Selenium，B 站空间主页必须）
selenium>=4.20.0,<5

# ========================= 可选 / 数据存储 =========================
# 配置 & 持久化（后期 settings.json 用 json 足够；若改用 yaml 就打开）
# PyYAML>=6.0

# 图像处理 / 抠图后期处理（如素材预处理需要 PIL）
# Pillow>=10.0.0

# ========================= 开发 & 打包 =========================
# 打包成 exe（开发时安装即可，不要求跟随项目分发）
# pyinstaller>=6.10
